[package]
name = "bridge-ffi"
version = "0.1.0"
edition = "2024"
description = "swift-bridge FFI layer for bridge-lite"

[lib]
crate-type = ["staticlib"]

[dependencies]
bridge-core = { path = "../bridge-core" }
