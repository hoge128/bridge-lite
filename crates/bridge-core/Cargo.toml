[package]
name = "bridge-core"
version = "0.5.1"
edition = "2024"
description = "bridge-lite core logic library (GUI-independent)"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
jpeg-encoder = "0.6"
kamadak-exif = "0.6.1"
libc = "0.2"
rayon = "1.11.0"
rusqlite = { version = "0.39.0", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1.50.0", features = ["full"] }
walkdir = "2.5.0"
xmp_toolkit = "1.12.1"

[dev-dependencies]
tempfile = "3"
