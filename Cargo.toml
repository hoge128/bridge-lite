[workspace]
members = [
    "crates/bridge-core",
    "crates/bridge-ffi",
]
resolver = "3"

[workspace.dependencies]
# 共通依存はここで一元管理 (各クレートで workspace = true で参照)

[patch.crates-io]
xmp_toolkit = { path = "vendor/xmp_toolkit" }
